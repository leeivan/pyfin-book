# 第2章 Python 基础语法与金融实例（章节文档）

## 前言

本章定位：本章建立语法基础，把金融公式、数据输入和结果展示转化为规范的 Python 程序。
本章按照“核心概念建立 -> 方法机制理解 -> 场景化实现 -> 实验巩固 -> 总结检查”的路径展开，确保读者从知识理解过渡到稳定实践。

目录导读：
1. 数据类型与变量
2. 运算符与表达式
3. 输入输出与格式化
4. 顺序结构
5. 实验与实践建议
6. 本章小结与学习检查

## 1. 数据类型与变量

本节聚焦“数据类型与变量”，从概念理解、实现路径与常见误区三个层面展开，为后续应用建立稳定基础。

### 1.1 核心概念与金融语义
- 使用金额、利率、期限等金融数据说明 `int`、`float`、`str` 的使用场景。
- 明确该主题在本章任务链中的输入、处理与输出关系。
- 用最小示例验证概念是否可执行，而非仅停留在定义层。

### 1.2 方法步骤与实现要点
- 强调浮点数精度问题，并引入“结果解释时保留小数位”的习惯。
- 说明变量命名、代码拆分和结果记录的最小规范。
- 强调先验证正确性，再逐步优化结构和可读性。

### 1.3 常见误区与实践提示
- 建立变量命名规范，如 `principal`、`rate`、`periods`。
- 区分“程序能运行”与“业务解释正确”两类不同的完成标准。
- 在每次运行后记录关键输入输出，便于复查与复现。

## 2. 运算符与表达式

本节聚焦“运算符与表达式”，从概念理解、实现路径与常见误区三个层面展开，为后续应用建立稳定基础。

### 2.1 核心概念与金融语义
- 实现单利、复利、收益率等基础计算表达式。
- 明确该主题在本章任务链中的输入、处理与输出关系。
- 用最小示例验证概念是否可执行，而非仅停留在定义层。

### 2.2 方法步骤与实现要点
- 对比“数学公式写法”与“Python 表达式写法”的差异。
- 说明变量命名、代码拆分和结果记录的最小规范。
- 强调先验证正确性，再逐步优化结构和可读性。

### 2.3 常见误区与实践提示
- 讨论括号优先级和错误表达式导致的业务偏差。
- 区分“程序能运行”与“业务解释正确”两类不同的完成标准。
- 在每次运行后记录关键输入输出，便于复查与复现。

## 3. 输入输出与格式化

本节聚焦“输入输出与格式化”，从概念理解、实现路径与常见误区三个层面展开，为后续应用建立稳定基础。

### 3.1 核心概念与金融语义
- 使用 `input()` 获取参数，完成基础交互式程序。
- 明确该主题在本章任务链中的输入、处理与输出关系。
- 用最小示例验证概念是否可执行，而非仅停留在定义层。

### 3.2 方法步骤与实现要点
- 使用 f-string 进行金额、百分比、对齐输出，形成报表可读性。
- 说明变量命名、代码拆分和结果记录的最小规范。
- 强调先验证正确性，再逐步优化结构和可读性。

### 3.3 常见误区与实践提示
- 介绍最小可用的结果校验思路（输入范围、类型转换异常）。
- 区分“程序能运行”与“业务解释正确”两类不同的完成标准。
- 在每次运行后记录关键输入输出，便于复查与复现。

## 4. 顺序结构

本节聚焦“顺序结构”，从概念理解、实现路径与常见误区三个层面展开，为后续应用建立稳定基础。

### 4.1 核心概念与金融语义
- 明确顺序结构的执行流程。
- 明确该主题在本章任务链中的输入、处理与输出关系。
- 用最小示例验证概念是否可执行，而非仅停留在定义层。

### 4.2 方法步骤与实现要点
- 用“从读取数据到输出报表”的单路径任务建立程序流程感。
- 说明变量命名、代码拆分和结果记录的最小规范。
- 强调先验证正确性，再逐步优化结构和可读性。

### 4.3 常见误区与实践提示
- 为后续分支和循环结构做认知铺垫。
- 区分“程序能运行”与“业务解释正确”两类不同的完成标准。
- 在每次运行后记录关键输入输出，便于复查与复现。

## 5. 实验与实践建议

本节通过分层实验把前四节知识转化为可验证的操作能力，建议按“基础复现 -> 功能完善 -> 结构优化”的顺序完成。

### 5.1 实验 1：实现复利终值计算器并支持用户输入
- 任务内容：实现复利终值计算器并支持用户输入。
- 最低要求：程序可独立运行，包含输入、处理、输出三个环节。
- 验收标准：提交运行结果与关键步骤说明，能够解释结果含义。

### 5.2 实验 2：输出“迷你资产收益报表”，包含本金、终值、收益率三列
- 任务内容：输出“迷你资产收益报表”，包含本金、终值、收益率三列。
- 最低要求：程序可独立运行，包含输入、处理、输出三个环节。
- 验收标准：提交运行结果与关键步骤说明，能够解释结果含义。

### 5.3 实验 3：制造一个故意错误表达式，比较错误结果与修正结果
- 任务内容：制造一个故意错误表达式，比较错误结果与修正结果。
- 最低要求：程序可独立运行，包含输入、处理、输出三个环节。
- 验收标准：提交运行结果与关键步骤说明，能够解释结果含义。

建议交付物：
- `basic_finance_calculator.py`
- `finance_report_printer.py`

## 6. 本章小结与学习检查

本节用于回收本章知识与实践成果，确认读者是否具备进入下一章的先修能力。

### 6.1 核心概念回顾
- 回顾本章四个核心主题：数据类型与变量、运算符与表达式、输入输出与格式化、顺序结构。
- 回顾从概念理解到程序实现、再到结果解释的完整学习路径。
- 强化“金融语义正确 + 代码实现正确”的双重标准。

### 6.2 学习检查清单
- 能否在不参考示例代码的情况下复现本章核心程序。
- 能否解释关键变量、核心公式和输出指标的业务含义。
- 能否对异常输入或边界情况给出合理处理。
- 进阶衔接：前置依赖：第1章开发环境与首个可运行脚本。
- 进阶衔接：后续衔接：第3章将在本章顺序结构基础上引入 `if/for/while` 实现业务规则判断与迭代计算。

### 6.3 常见错误与纠正建议
- 本章难点在“语法细节 + 金融含义”的双重正确性。
- 建议每个公式都先口头解释变量含义，再进入代码实现。
- 建议每次实验都保留输入参数与输出结果，形成可追溯记录。
- 建议在提交前进行一次“结果复核 + 解释复核”的双重检查。


