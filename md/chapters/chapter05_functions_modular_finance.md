# 第5章 函数与模块化金融程序设计（章节文档）

## 前言

本章定位：本章将“脚本堆叠”升级为“可复用模块”，建立工程化组织代码的基础能力。
本章按照“核心概念建立 -> 方法机制理解 -> 场景化实现 -> 实验巩固 -> 总结检查”的路径展开，确保读者从知识理解过渡到稳定实践。

目录导读：
1. 函数定义与参数传递
2. 金融计算函数封装
3. 模块与包
4. 案例：金融计算工具库设计
5. 实验与实践建议
6. 本章小结与学习检查

## 1. 函数定义与参数传递

本节聚焦“函数定义与参数传递”，从概念理解、实现路径与常见误区三个层面展开，为后续应用建立稳定基础。

### 1.1 核心概念与金融语义
- 讲解位置参数、默认参数、返回值设计。
- 明确该主题在本章任务链中的输入、处理与输出关系。
- 用最小示例验证概念是否可执行，而非仅停留在定义层。

### 1.2 方法步骤与实现要点
- 强调函数职责单一与命名表达业务语义。
- 说明变量命名、代码拆分和结果记录的最小规范。
- 强调先验证正确性，再逐步优化结构和可读性。

### 1.3 常见误区与实践提示
- 通过输入校验提升函数健壮性。
- 区分“程序能运行”与“业务解释正确”两类不同的完成标准。
- 在每次运行后记录关键输入输出，便于复查与复现。

## 2. 金融计算函数封装

本节聚焦“金融计算函数封装”，从概念理解、实现路径与常见误区三个层面展开，为后续应用建立稳定基础。

### 2.1 核心概念与金融语义
- 封装终值、现值、年化收益率等函数。
- 明确该主题在本章任务链中的输入、处理与输出关系。
- 用最小示例验证概念是否可执行，而非仅停留在定义层。

### 2.2 方法步骤与实现要点
- 讨论参数单位统一（年/月、百分比/小数）问题。
- 说明变量命名、代码拆分和结果记录的最小规范。
- 强调先验证正确性，再逐步优化结构和可读性。

### 2.3 常见误区与实践提示
- 通过文档字符串明确公式与参数约定。
- 区分“程序能运行”与“业务解释正确”两类不同的完成标准。
- 在每次运行后记录关键输入输出，便于复查与复现。

## 3. 模块与包

本节聚焦“模块与包”，从概念理解、实现路径与常见误区三个层面展开，为后续应用建立稳定基础。

### 3.1 核心概念与金融语义
- 演示 `import`、模块拆分、目录组织。
- 明确该主题在本章任务链中的输入、处理与输出关系。
- 用最小示例验证概念是否可执行，而非仅停留在定义层。

### 3.2 方法步骤与实现要点
- 介绍 `__init__.py` 的基础作用和接口暴露方式。
- 说明变量命名、代码拆分和结果记录的最小规范。
- 强调先验证正确性，再逐步优化结构和可读性。

### 3.3 常见误区与实践提示
- 说明“主程序与工具模块分离”的维护价值。
- 区分“程序能运行”与“业务解释正确”两类不同的完成标准。
- 在每次运行后记录关键输入输出，便于复查与复现。

## 4. 案例：金融计算工具库设计

本节聚焦“案例：金融计算工具库设计”，从概念理解、实现路径与常见误区三个层面展开，为后续应用建立稳定基础。

### 4.1 核心概念与金融语义
- 构建 `finance_utils` 包并实现核心函数。
- 明确该主题在本章任务链中的输入、处理与输出关系。
- 用最小示例验证概念是否可执行，而非仅停留在定义层。

### 4.2 方法步骤与实现要点
- 编写示例调用脚本验证可复用性。
- 说明变量命名、代码拆分和结果记录的最小规范。
- 强调先验证正确性，再逐步优化结构和可读性。

### 4.3 常见误区与实践提示
- 预留后续章节可扩展接口（风险指标、回测函数）。
- 区分“程序能运行”与“业务解释正确”两类不同的完成标准。
- 在每次运行后记录关键输入输出，便于复查与复现。

## 5. 实验与实践建议

本节通过分层实验把前四节知识转化为可验证的操作能力，建议按“基础复现 -> 功能完善 -> 结构优化”的顺序完成。

### 5.1 实验 1：把第2-4章脚本中的重复计算提炼成函数
- 任务内容：把第2-4章脚本中的重复计算提炼成函数。
- 最低要求：程序可独立运行，包含输入、处理、输出三个环节。
- 验收标准：提交运行结果与关键步骤说明，能够解释结果含义。

### 5.2 实验 2：构建 `finance_utils` 并写最少 5 个函数
- 任务内容：构建 `finance_utils` 并写最少 5 个函数。
- 最低要求：程序可独立运行，包含输入、处理、输出三个环节。
- 验收标准：提交运行结果与关键步骤说明，能够解释结果含义。

### 5.3 实验 3：为每个函数补充输入异常处理与说明文档
- 任务内容：为每个函数补充输入异常处理与说明文档。
- 最低要求：程序可独立运行，包含输入、处理、输出三个环节。
- 验收标准：提交运行结果与关键步骤说明，能够解释结果含义。

建议交付物：
- `finance_utils/`（含核心金融计算函数）
- `finance_utils_demo.py`

## 6. 本章小结与学习检查

本节用于回收本章知识与实践成果，确认读者是否具备进入下一章的先修能力。

### 6.1 核心概念回顾
- 回顾本章四个核心主题：函数定义与参数传递、金融计算函数封装、模块与包、案例：金融计算工具库设计。
- 回顾从概念理解到程序实现、再到结果解释的完整学习路径。
- 强化“金融语义正确 + 代码实现正确”的双重标准。

### 6.2 学习检查清单
- 能否在不参考示例代码的情况下复现本章核心程序。
- 能否解释关键变量、核心公式和输出指标的业务含义。
- 能否对异常输入或边界情况给出合理处理。
- 进阶衔接：前置依赖：第4章数据结构组织方式。
- 进阶衔接：后续衔接：第6章将把函数与文件操作结合，形成“读入-处理-写出”流程。

### 6.3 常见错误与纠正建议
- 本章评价标准应从“能算”提升到“可复用、可读、可维护”。
- 建议引入简单单元测试思想，但不强制完整测试框架。
- 建议每次实验都保留输入参数与输出结果，形成可追溯记录。
- 建议在提交前进行一次“结果复核 + 解释复核”的双重检查。


